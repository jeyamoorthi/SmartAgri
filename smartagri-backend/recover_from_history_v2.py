"import json\nimport os\nimport re\nimport ast\n\nbrain_dir = r\"C:\\Users\\Jayamoorthi\\.gemini\\antigravity\\brain\"\nworkspace_dir = r\"C:\\Users\\Jayamoorthi\\OneDrive\\Desktop\\azuo\\SMARTAGRI\"\n\nfile_versions = {} # path -> list of (length, content, source_type, step_info)\n\ndef clean_arg(val):\n    if isinstance(val, str):\n        val_strip = val.strip()\n        if val_strip.startswith('\"') and val_strip.endswith('\"'):\n            try:\n                # Use ast.literal_eval to evaluate python string literal\n                return ast.literal_eval(val_strip)\n            except Exception:\n                # Fallback manual replacement\n                return val_strip[1:-1].replace('\\\\n', '\\n').replace('\\\\\"', '\"').replace('\\\\\\\\', '\\\\')\n    return val\n\ndef clean_view_content(content):\n    lines = content.split('\\n')\n    code_lines = []\n    in_code = False\n    for line in lines:\n        if line.startswith(\"The following code has been modified\"):\n            in_code = True\n            continue\n        if in_code:\n            match = re.match(r'^\\d+:(?: (.*))?$', line)\n            if match:\n                val = match.group(1)\n                code_lines.append(val if val is not None else \"\")\n            elif line.strip() == \"\" or line.startswith(\"The above content shows\"):\n                continue\n            else:\n                pass\n    if code_lines:\n        return '\\n'.join(code_lines)\n    return None\n\n# Find all transcript.jsonl files\nfor d in os.listdir(brain_dir):\n    d_path = os.path.join(brain_dir, d)\n    if not os.path.isdir(d_path):\n        continue\n    \n    transcript_path = os.path.join(d_path, \".system_generated\", \"logs\", \"transcript.jsonl\")\n    if not os.path.exists(transcript_path):\n        continue\n        \n    print(f\"Scanning transcript in {d}...\")\n    with open(transcript_path, 'r', encoding='utf-8', errors='ignore') as f:\n        for line_num, line in enumerate(f):\n            try:\n                obj = json.lo
<truncated 3261 bytes>