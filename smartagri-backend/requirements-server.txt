# SmartAgri Backend Requirements — Server Only
# Use this in Docker or headless environments where audio recording isn't needed
# Remove: pyaudio (requires PortAudio dev libraries - complex to install)

# ──────────────────── CORE FASTAPI ───────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.0
python-multipart==0.0.9

# ──────────────────── DATABASE ────────────────────────────────────
motor==3.5.0
pymongo[srv]==4.8.0
dnspython==2.6.1

# ──────────────────── AUTHENTICATION ──────────────────────────────
bcrypt==4.2.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
email-validator==2.2.0

# ──────────────────── SCHEDULING ─────────────────────────────────
apscheduler==3.10.4

# ──────────────────── AI SERVICES ────────────────────────────────
google-generativeai==0.8.0
groq==0.4.1

# ──────────────────── HTTP & NETWORKING ─────────────────────────
requests==2.31.0
httpx==0.27.0
aiosmtplib==3.0.1

# ──────────────────── CONFIGURATION ──────────────────────────────
python-dotenv==1.0.1

# ──────────────────── VOICE & AUDIO (server-side only) ──────────
# Speech recognition from WebM/MP4 audio
SpeechRecognition==3.10.1
# Audio format conversion
pydub==0.25.1
# NO pyaudio - not needed for server (graceful fallback)
# Text-to-speech synthesis
gTTS==2.4.0
# Text processing for NLTK-based BLEU scoring
nltk==3.8.1

# ─────────────────────────────────────────────────────────────────────────
# NOTES:
# - pyaudio removed: Not needed on server, complex build in Docker
# - keyboard removed: Not needed in server environments
# - Audio playback works via gTTS or Bhashini TTS (returns audio bytes)
# - Audio recording handled by Bhashini ASR (receives audio from client)
#
# To use full dev environment with audio recording support, use:
#   pip install -r requirements.txt (includes pyaudio)
# ─────────────────────────────────────────────────────────────────────────
